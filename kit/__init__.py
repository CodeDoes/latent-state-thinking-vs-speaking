"""kit — tier-0 primitives shared by every thread.

runlog: experiment artifact writer (config/log/metrics/checkpoint)
tasks:  synthetic tasks with learnable patterns (smoke tests)
train:  THE training loop + batching/param helpers
nn:     shared neural blocks (LoRALinear, ...)
"""
